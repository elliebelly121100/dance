<h1>Users</h1>

<p><a href="/new_user">Add a New User</a></p>

<table>
  <thead>
    <tr>


      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
  <% @users.each do |user| %>
    <tr>
      <th>Picture</th>
      <td><img class="profile"src="<%= user.img %>"><td>
      </tr>
    <tr>
      <th>Email</th>
      <td><%= user.name %><td>
      </tr>
    <tr>
      <th>Bio</th>
      <td><%= user.bio %></td>
      </tr>
          <tr>
            <td><a href="/users/<%= user.id %>">Show</a> |
              <a href="/users/<%= user.id %>/edit">Edit</a> |
            <a href="/users/<%= user.id %>/destroy">Destroy</a></td>
          </tr>
  <% end %>
  </tbody>
</table>

